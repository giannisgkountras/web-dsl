import accessors
import components
import entity
import conditions


Repetition:
    (
        ('for' 'item' item=NestedAccessPath)
        ('in' 'entity' entity=[Entity])
        ('use' component=RepetitionComponentTypes )
        ('with' data=NestedAccessPathItem)?
        ('if' condition=HalfExpression)?
        ('else' 'use' componentElse=RepetitionComponentTypes)?
    )#
    
;

RepetitionComponentTypes: 
    Text | Gauge | Image | ComponentRef
;