import accessors
import components
import entity
import conditions


Repetition:
    (
        ('for' item=NestedAccessPath)
        ('in' 'entity' entity=[Entity])
        ('use' component=RepetitionComponentTypes)
        ('with' 'data' data=NestedAccessPath)?
        ('if' condition=HalfExpression)?
    )#
    
;

RepetitionComponentTypes: 
    Text | Gauge | Image
;